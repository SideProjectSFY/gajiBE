<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gaji.corebackend.mapper.ConversationMemoMapper">

    <resultMap id="ConversationMemoResultMap" type="com.gaji.corebackend.entity.ConversationMemo">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="conversationId" column="conversation_id"/>
        <result property="content" column="content"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="findById" resultMap="ConversationMemoResultMap">
        SELECT * FROM conversation_memos WHERE id = #{id}
    </select>

    <select id="findByUserIdAndConversationId" resultMap="ConversationMemoResultMap">
        SELECT * FROM conversation_memos WHERE user_id = #{userId} AND conversation_id = #{conversationId}
    </select>

    <select id="existsByUserIdAndConversationId" resultType="boolean">
        SELECT EXISTS(SELECT 1 FROM conversation_memos WHERE user_id = #{userId} AND conversation_id = #{conversationId})
    </select>

    <delete id="deleteByUserIdAndConversationId">
        DELETE FROM conversation_memos WHERE user_id = #{userId} AND conversation_id = #{conversationId}
    </delete>

    <insert id="insert" parameterType="com.gaji.corebackend.entity.ConversationMemo">
        INSERT INTO conversation_memos (
            id, user_id, conversation_id, content, created_at, updated_at
        ) VALUES (
            #{id}, #{userId}, #{conversationId}, #{content}, NOW(), NOW()
        )
    </insert>

    <update id="update" parameterType="com.gaji.corebackend.entity.ConversationMemo">
        UPDATE conversation_memos SET
            content = #{content},
            updated_at = NOW()
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM conversation_memos WHERE id = #{id}
    </delete>

    <select id="existsById" resultType="boolean">
        SELECT EXISTS(SELECT 1 FROM conversation_memos WHERE id = #{id})
    </select>

</mapper>
