<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gaji.corebackend.mapper.BookLikeMapper">

    <select id="existsByUserIdAndBookId" resultType="boolean">
        SELECT EXISTS(SELECT 1 FROM book_likes WHERE user_id = #{userId} AND book_id = #{bookId})
    </select>

    <select id="findBookIdsByUserId" resultType="java.util.UUID">
        SELECT book_id FROM book_likes WHERE user_id = #{userId}
    </select>

    <select id="findBookIdsByUserIdPaged" resultType="java.util.UUID">
        SELECT book_id FROM book_likes 
        WHERE user_id = #{userId}
        ORDER BY created_at DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countByBookId" resultType="long">
        SELECT COUNT(*) FROM book_likes WHERE book_id = #{bookId}
    </select>

    <select id="countByUserId" resultType="long">
        SELECT COUNT(*) FROM book_likes WHERE user_id = #{userId}
    </select>

    <insert id="insert" parameterType="com.gaji.corebackend.entity.BookLike">
        INSERT INTO book_likes (
            user_id, book_id, created_at
        ) VALUES (
            #{userId}, #{bookId}, NOW()
        )
    </insert>

    <delete id="deleteByUserIdAndBookId">
        DELETE FROM book_likes WHERE user_id = #{userId} AND book_id = #{bookId}
    </delete>

</mapper>
